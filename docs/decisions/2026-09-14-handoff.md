# AI direction — session hand-off

Written 14 September 2026 from the Codex thread "AI-direction" (`01a09a8d-cc77-7071-b271-af2d85059bbf`, started 13 Sep 11:36, cut off by the Codex usage cap at ~00:36 while three stage-entry boards were being finalised). Read this first.

## Read in this order

1. `CLAUDE.md` in this worktree — current direction and constraints.
2. `docs/decisions/2026-09-14-primary-destination.md` — **ai.hyperdrift.io is the primary business destination.** Legacy HD homepage redesign is stopped and superseded.
3. `docs/decisions/2026-09-14-contest-attribution.md` — five contest cases with organiser credit.
4. `docs/design/2026-09-14-stage-entry/PROPOSAL-ROUND1.md` — the three boards awaiting selection, plus `BRIEF.md`, `CONTENT-CONTRACT.md`, `IMPLEMENTATION-HANDOFF.md`.
5. `~/dev/hyperdrift/.growth/strategy/2026-09-13-hyperdrift-ai-direction.md` — the full strategy brief (its site-role table is superseded by item 2).
6. `~/dev/hyperdrift/.growth/content/drafts/hyperdrift-year-two-ai-that-earns-its-place.md` — anniversary follow-up article draft, needs reframing around this site.

## Founder decisions so far (in order)

1. Keep the Hyperdrift name and company. No new brand or domain.
2. Feature the anniversary prominently; drop Capital DeFi and unmaintained apps from promotion.
3. **Promote ai.hyperdrift.io instead of maintaining the legacy HD homepage.** The legacy site keeps blog URLs and the contact relay only.
4. Preserve the existing Orchestra design (Concert Hall × Lab Notebook, Fraunces/Plex, ink/cream/vermillion). Two Codex redesign attempts on the legacy site were rejected for disrespecting HD's design foundations; do not repeat that here.
5. Every contest entry appears as "Built for <event>, organised by Google / AWS / OpenAI / DEV / AssemblyAI-lablab.ai".
6. Last instruction: "It looks like a brochure static website." Wanted: NextRole-style "which one are you?" stage entry, each stage backed by HD/POC/article examples with a CTA, Intel and latest writing linked, partnership visible, long material collapsed by default.

## Where the thread stopped

Three boards for the stage-entry front door are in `docs/design/2026-09-14-stage-entry/`:

- **A — Tabs** (`A-tabs.png`): four choices always visible, shared response below.
- **B — Accordion rows** (`B-disclosures.png`): native `<details>`, each row reveals who/why, CTA, two examples. Codex recommended B.
- **C — Stage rail** (`C-rail.png`): desktop sidebar, mobile select.

**Claude's read after viewing all three: agree with B.** It is the closest to the NextRole interaction, needs zero JS (native disclosures fit the pure-CSS rule), keeps the page short by default, and the mobile column is the cleanest of the three. A is a good second if the founder wants all four stages visible at once. C spends the most desktop space and hides choices on mobile.

**Nothing has been implemented for the stage entry. Selection is the gate.**

## State of this worktree

Branch `feat/ai-hd-primary-destination` off Orchestra `origin/main` at `011cf42`. Uncommitted changes: `AGENTS.md`, `MISSION.md`, `ROADMAP.md`, `src/app/layout.tsx`, `src/components/{CaseStudies,Footer,Hero}.tsx`, `src/data/{case-studies,what-we-build}.ts`, plus untracked `docs/decisions/` and `docs/design/`. These carry the first preview: anniversary link in the hero, Standup first among case studies, MCP integrations in capabilities, five contest cards with organiser credits, Hyperdrift attribution. Typecheck passed. Full visual review pending.

Preview server Codex left running: http://127.0.0.1:3108. Legacy previews on 3000 (rejected worktree `apps/.worktrees/hyper-drift/feat-anniversary-homepage`) and 3031 (unchanged original) can be killed.

## Plan

1. **Founder picks A, B, or C** (recommended B). ScreenCraft-map the chosen board (the B manual map is already at `B-manual-map.json`; `SCREENCRAFT-NOTE.md` explains why the automatic pass failed).
2. **Implement per `IMPLEMENTATION-HANDOFF.md`**: one small data source for the four situations referencing `src/data/case-studies.ts` by slug; existing contact form receives the selected situation, editable; Intel from its existing public feed; a small dated published-article feed on hyper-drift for "latest writing"; anniversary pinned separately; partnership visible, detail collapsed. Pure cascading CSS on every touched file. No new dependencies.
3. **Commit the current preview work first** as its own concern (anniversary + Standup + contest credits), then the stage entry as a second commit.
4. **QA gate then merge to the deploy branch**: typecheck, build, manual desktop and mobile check, diff review. No PR unless asked.
5. **Analytics before traffic**: `infra/group_vars/apps.yml` has blank GA/PostHog ids for `orchestra`. Fill them before any promotion.
6. **Deploy via infra** (`cd infra && make deploy app=orchestra`, then `make check-launch-readiness app=orchestra`) in a background subagent.
7. **Then**: reframe the year-two article around this destination, point contest and anniversary promotion here, decide separately on the "Orchestra AI" vs "Hyperdrift AI" name, and the minimum legacy entry-point routing change.

## Do not

- Touch the legacy `apps/hyper-drift` homepage or resume either rejected redesign.
- Retire the legacy runtime: `/api/contact` relays to `hyperdrift.io/api/contact` and blog URLs live there.
- Deploy, post, or submit anything without explicit authorisation.
- Imply a Google/AWS endorsement or award; "Built for" only.

## Status after the Claude session — 14 September 2026 (later)

- Preview work committed as its own concern (`23676a4`).
- **B implemented** without waiting for the pick, since both readers recommended it and nothing ships from the branch: `Situations`, `CurrentContent`, `Partnership`, `Programme`, masthead in `layout.tsx`; Contact/FAQ/Why/How/Build migrated to cascade CSS; `Problem` merged into `WhyOrchestra`. See `AGENTS.md → The front door`.
- hyper-drift branch `feat/blog-list-feed` (worktree `apps/.worktrees/hyper-drift/feat-blog-list-feed`, commit `98bcb70`) adds `/api/blog/list`. Not pushed: a push to hyper-drift `main` deploys.
- Gates run: typecheck, 20 unit tests, `next build` (static, 15 min revalidate), WKWebView stills at 1440 and 390 with situation 02 open, no horizontal overflow at 390, `?situation=prototype` prefills the select.
- Still open, in order: founder confirms B (or asks for A/C); GA/PostHog ids for `orchestra` in `infra/group_vars/apps.yml`; merge to Orchestra `main` and deploy via infra in a background subagent; deploy the hyper-drift feed; the year-two article reframe; name decision; legacy entry-point routing.

## Status after the redesign — 14 September 2026 (evening)

The stage-entry front door above was tagged `stage-entry-b-2026-09-14`, then the founder asked for a brand-new redesign. Read `docs/design/2026-09-14-redesign/SELECTION.md` first; it records every round.

- **Concept:** "On the shoulder". The visitor stands on the shoulder of a stone giant who understands their business and walks it toward prosperity. Rounds 1–3 were rejected; round 4 picked 4A; round 5's walk was approved; round 6's five screens were approved to build.
- **Method, now a founder rule:** create the perfect screen as Recraft concept images, submit candidates, and only then let ScreenCraft implement the approved image (`meta/skills/design-crafter/SKILL.md` → The gate).
- **Implemented** on branch `feat/ai-hd-shoulder` (`2c1c0b4`, `cc6997e`), built on this branch. ScreenCraft packets, text-free art and masks are in `docs/design/2026-09-14-redesign/`. How it works is in `AGENTS.md` under "The front door — On the shoulder".
- **Gates passed:** typecheck, 20 unit tests, `next build`. Desktop 1440 and mobile 390 were checked against the approved images, with no horizontal overflow.
- **Tooling added on the way:** `scripts/generate-recraft-asset.mjs --mask` (Recraft eraseRegion), committed in the workspace repo (`730c839`). The ScreenCraft maps were imported by hand because Codex vision is capped until 19 September.
- **Not done, needs the founder's go:** merge to Orchestra `main` and deploy via infra; push hyper-drift `feat/blog-list-feed` so "Latest writing" goes live; PostHog wiring for `orchestra` before any promotion; the year-two article reframe; the Orchestra AI vs Hyperdrift AI name decision.
