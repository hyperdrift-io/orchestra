# Orchestra AI — negative-space mark review

22 September 2026. Founder request: create an outstanding negative-space logo. Existing Orchestra AI naming, Hyperdrift ownership and gold/ink/ivory identity remain the brief. The homepage refinement is already authorized; the founder selected **A — Aperture** with “A is good.” The approval includes the vector refinement and site application proposed alongside the concepts.

| Direction | Image | Strength | Trade-off |
|---|---|---|---|
| A — Aperture | [Review](a-aperture.png) | The O silhouette reveals an A through negative space; direct name recognition and strong reduction. | Architectural character; optical refinement needed at very small sizes. |
| B — Confluence | [Review](b-confluence.png) | A clear ascending path joins two forms; compact and distinctive. | Meaning is more abstract than a monogram. |
| C — Resonance | [Review](c-resonance.png) | Coordinated curves reveal a central core, relating to the living business. | More abstract and less explicit about the name. |

Selection: **A**, for the clearest connection to Orchestra AI. The proofs remain the design record. A now has a flat vector master at `public/brand/orchestra-aperture.svg`, a current-colour monochrome version and a matching `src/app/icon.svg` favicon. True transparent cutouts preserve the O/A countershape on any background; no raster texture or lighting is carried into the master. The masthead and footer retain live text with decorative empty-alt symbols, so each home link has one clear accessible name.

Prompts and generation notes: [A](a-aperture.md), [B](b-confluence.md), [C](c-resonance.md). Images generated with the built-in image generation tool; final review copies are kept here. Implementation: local preview; no deployment.

Verification: typecheck and diff whitespace check passed. Browser review at 1280px and 390px confirms the gold SVG loads at its square 44px header size, the wordmark remains readable and the page has no horizontal overflow. The footer asset loads at 48px. The registered SVG favicon returns HTTP 200. Preview remains http://127.0.0.1:3112/.

## Open Graph application

The founder requested the sharing-image update after approving Aperture. The default 1200 × 630 PNG uses the vector master, the site's Cormorant/Plex typography, ink background and gold growth/profit proposition. [Rendered preview](opengraph-preview.png). Generated by the existing `next/og` renderer at `/opengraph-image`; local font files and their licenses are recorded in `src/assets/share-fonts/`.

Homepage, Work, How it works, Partnership and the article index carry the branded image in Open Graph and large-image Twitter metadata. Child pages define their image explicitly because their Open Graph objects replace the parent's object. Article detail pages retain their individual headline, action sequence and evidence attribution; each article card now includes the approved mark.

Local verification: all six sampled page routes return HTTP 200 with an image URL and 1200 × 630 dimensions. Default, expertise and longest-headline delegation PNGs return HTTP 200 and have been visually inspected for clipping, proportions and contrast. Typecheck and an isolated production build passed. The generated production homepage points to the HTTPS image on ai.hyperdrift.io. This update remains a local preview.
