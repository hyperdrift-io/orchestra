# B — Accordion rows: final image appraisal

The final board retains Orchestra's ink/cream/vermillion identity, expressive serif hero, outlined movement numbers, musical rules and direct enquiry action. It makes the visitor's situation the entry point, with evidence and an action revealed beside the relevant explanation.

## Verification after repair

- Desktop and mobile both show all four named stages, in order.
- Stage 02 is open; its outcome, enquiry button, two examples and approach disclosure are inside row 02 before closed row 03.
- Stages 01, 03 and 04 each have a plus; only 02 has a minus.
- Both viewports show Intel, recent writing, the Traction Partnership, and the closed supporting sections.
- Evidence links and organiser attribution are readable; duplicate and garbled draft text was removed.
- Supporting body was shortened faithfully: “For teams with a repeatable task. Connect your tools, with people in control.”

Minor implementation corrections: the closed 03 numeral still appears vermillion (use the closed-state neutral colour). Mobile includes a decorative hamburger even though all three navigation links are visible; omit that redundant control. The mock adds a faint button texture and a rounded page frame; keep the existing site's plain vermillion button and treat the frame as board presentation, not a new site shell.

This is the expanded demonstration state. The actual B entry starts with all stages closed, per BRIEF.md. Opening any stage reveals its own relevant evidence and action.

## Provenance

The first Recraft render failed the accordion structure and omitted important mobile content. It is preserved as `B-draft.png`, with original seed/model metadata in `B-draft.json` and original prompt in `B-disclosures.prompt.txt`. One image_gen edit repaired that same board, using `B-repair.prompt.txt`. The final file is `B-disclosures.png`, and `B-disclosures.json` records its repair provenance. No UI files or production assets were edited.
