# Selected: A — Living constellation

The founder replied **“A”** to the three-direction review on 22 September 2026. This authorizes implementation of the Living constellation direction. The selected original is [ecosystem.png](ecosystem.png). B and C remain unselected references.

## Implemented scope

- `/how-it-works` replaces the boxed organisation diagram with five coloured, luminous spherical domains in the selected asymmetric constellation. Fine interior blooms, shared branching paths, moving light and a central memory field are generated with the existing Three.js dependency.
- A signal can activate several related routes at once. Particles follow the same Bezier curves as the illuminated filaments. Click a sphere or junction to inspect the receiving domain's signal, action, business impact and measure. Click outside or use Escape/back to return.
- Product, service and consultancy scenarios each have three named illustrative improvements. Each successful illustrated cycle retains a memory ring and its outcome in the page. After all three, existing signals continue circulating without incrementing the number of improvements. This depicts the mechanism, not measured client revenue.
- Phone layout preserves five spheres in a more vertical composition; focused business details flow below the canvas. Desktop details sit beside the entered sphere. Native buttons support keyboard and touch access; reduced motion stops animated travel and uses immediate camera changes. Offscreen/hidden scenes pause their animation work.
- Home now contains the growth/profit promise, one interactive mesh, Standup/Helm/Radar proof and enquiry. The detailed organisation, six-article grid, news feed and partnership block no longer interrupt that flow.
- `/work` retains all five case studies, recordings, public source links, challenge credit and prototype status. `/partnership` retains the offer and links to the enquiry with its situation preselected. Article navigation respects draft visibility and falls back to published Hyperdrift writing when the series is unavailable.
- Site navigation, footer, canonical metadata and sitemap include the new destinations. No public content was deployed or submitted to search engines in this local preview turn.

## Design mapping and fidelity

[ScreenCraft map](screencraft/design-map.json), [annotated reference](screencraft/design-overlay.png) and [mapping notes](screencraft/DESIGN.md) preserve the approved hierarchy and sphere regions. The mapping image and overlay were inspected. Observed composition: centred ivory serif title, five coloured spheres surrounding a gold branching centre, and dark negative space. Business labels, controls, scenario narration, focused panels and mobile layout are implementation inferences from the approved interaction requirements.

This is a procedural interpretation of the selected image. It preserves the colour mapping, silhouette and connected material, rather than reproducing the image model's photographic filaments pixel for pixel. There is no flattened concept image behind the interactive canvas.

The first visual pass made the spheres too small and the headline too tall. The reconciliation enlarged the spheres, restored the one-line desktop title, spread the paths, strengthened the light and moved focused business text beside the domain. Phone focus uses a shorter canvas and natural text flow to avoid overlap.

ScreenCraft runtime: invoke with `HD_PYTHON=/Users/yannvr/dev/hyperdrift/scripts/.venv/bin/python`. The default interpreter lacked jsonschema. The bounded vision output included an imagined detail panel without source evidence and failed map validation; a manually inspected evidence map was imported using the existing `--from-json` path. Hidden interaction states are recorded as specifications, not asserted as visible evidence. No CLI/dependency change was needed.

## Validation

- `npm run typecheck` and `git diff --check` passed. Next's generated route types refreshed after the dev server first compiled the new routes.
- `/`, `/how-it-works`, `/work` and `/partnership` return 200. New routes have canonical metadata and appear in `/sitemap.xml`. Draft articles remain excluded.
- Manual desktop 1440px and phone 390px inspection: no horizontal overflow; domain colours, facet positions, story panel and native targets remain readable.
- Verified domain entry and business-impact selection; Escape restores focus to the selected domain; tapping canvas space outside the sphere returns to the overview.
- Verified service-scenario change, third-iteration selection and two earlier retained improvements. Automatic cycles were observed advancing and retaining all three before continuing ambient signal circulation.
- Home has exactly three main sections (`system`, `proof`, `contact`) and three featured builds. `/work` retains five. Partnership navigation preselects `situation=partnership` in the enquiry. No form was submitted.
- Browser inspection reported no renderer or console errors. Reduced-motion, pause, offscreen and resource cleanup paths were reviewed in code. No automated product tests or production performance claim.

## Delivery state

Local preview remains running at http://127.0.0.1:3112/. Review the constellation at http://127.0.0.1:3112/how-it-works. No push or deployment. Infra is unchanged because these are local UI/content routes; production release and sitemap submission remain separate work. Analytics is still a publication prerequisite, so the shortened hierarchy is a conversion hypothesis, not measured lift.
